#!/usr/bin/env ruby

require_relative "../lib/normalize"

project, bucket = ARGV

unless project && bucket && ARGV.size == 2
  abort "Usage: ruby bin/normalize [project] [bucket]"
end

Cloud::Normalize.call(project, bucket)
